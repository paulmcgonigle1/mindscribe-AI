{
    "name": "mindscribe-ai",
    "version": "1.0.0",
    "scripts": {
      "build": "cd clientside && npm install && npm run build",
      "postinstall": "npm install --prefix clientside"

    },
    "engines": {
      "node": "14.x"
    }
  }